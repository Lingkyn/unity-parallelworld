# AI协作开发设计流

# AI 驱动的架构陪练系统

# 人 × AI 协作式功能设计流（v2）

目标：

- **你**：写需求、拍板决策、验收实现

- **AI**：补全缺口、产出设计、生成实现交接清单、按清单实现

- 每步有清晰输入/输出，减少来回，降低实现后修 bug 的时间

适用定位：

- 你与 AI 协作开发 Unity 功能

- 设计产出可直接交给 AI 实现

────────────────────────────────────

# 协作原则

1. **你写一次**：原始功能卡片（业务意图）

2. **AI 补全**：缺口、增强卡片、设计、实现交接清单

3. **你确认**：STEP 0.6 待确认清单；必要时 STEP 1 关键决策

4. **AI 实现**：基于实现交接清单编码，按验证项自检

5. **单线引用**：除 STEP 0.5 外不改需求；设计只引用增强版功能卡片

────────────────────────────────────

# 角色与交接

| 阶段 | 你             | AI                   | 产出给谁用     |
| ---- | -------------- | -------------------- | -------------- |
| 需求 | 写原始功能卡片 | —                    | AI             |
| 补全 | 可选补充       | 缺口、增强卡片       | 你 + AI        |
| 决策 | 确认 STEP 0.6  | 生成方案             | 双向           |
| 设计 | 可选选决策     | 架构、接口、实现约束 | AI             |
| 交接 | —              | 实现交接清单         | AI（实现必读） |
| 实现 | 验收           | 按清单编码           | 你             |

**给实现阶段 AI 的固定指令**（实现时请附上）：

> 请基于《实现交接清单》《增强版功能卡片》《接口合同》实现。**实现前**：必须检查文档中的待确认清单、实现约束、Config/验证项是否均已标注「✓ 已确认」或「你的选择」；若有未确认项，**先列出并询问用户**，待用户补齐后再开始编码。**开始执行前**：必须先与用户确认是否开始执行实现（例如列出即将执行的步骤或待办），待用户明确同意（如回复「开始」「按清单执行」等）后再动手编码，不得未经确认直接开始改代码。实现交接清单中的「关键实现要点」和「验证项」为强制要求。若功能涉及**多段时序/协程等待**，实现时需按交接清单中的**顺序与 Config 字段**依次实现，并确保各段等待使用对应 Config 值而非硬编码。完成后请对照验证项自检。

────────────────────────────────────

# 你需要查看与特别关注的内容

AI 生成内容较多，以下是你**必须或建议查看**的部分，其余可快速浏览或跳过。

| 步骤                      | 你必须/应查看      | 特别关注                                                                                               |
| ------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------ |
| **STEP 0 缺口清单**       | 建议扫一眼         | **第 8 类「实现风险缺口」**：若涉及 UI、资产、Config、相机，这里会暴露易踩坑点，遗漏会导致实现后修 bug |
| **STEP 0.5 增强卡片**     | 建议扫一眼         | **「待确认」占位**、**规则与边界** 是否符合你预期；若有误，在 STEP 0.6 前让 AI 修正                    |
| **STEP 0.6 待确认清单**   | **必须查看并确认** | 每一条都是需你拍板的决策；回复「按建议来」或逐条确认后再进 STEP 1                                      |
| **STEP 1 设计审查**       | **必须查看**       | **「实现约束」**（必看）、**关键架构决策**：若与既有系统/镜头/Config 冲突，需你指出                    |
| **STEP 2–4**              | 可选               | 模块划分、状态转换是否合理；有疑问可让 AI 调整                                                         |
| **STEP 7 接口合同**       | 建议查看           | 与既有模块的接口是否冲突；ScriptableObject Entry 结构是否符合你项目                                    |
| **STEP 8.5 实现交接清单** | **必须查看**       | **Config/资产检查清单**：必填项、常见错误；**实现后验证项**：是否符合你的验收标准；不符需让 AI 修改    |
| **实现完成后**            | **必须执行**       | 按实现交接清单中的**验证项**逐条验收；可选让 AI 产出《XXX 使用说明》便于日后查阅（见「功能文档结构建议」第 9 条） |

**一句话**：STEP 0.6、STEP 1 实现约束、STEP 8.5 交接清单，这三处必须你过目确认；其余按需。

────────────────────────────────────

# AI 产出时的固定要求：附带《你需要查看与特别关注的内容》

**规则**：每当 AI 产出较长文档（缺口清单、增强卡片、设计审查、实现交接清单等）时，**必须同时在回复开头或单独一块**产出《你需要查看与特别关注的内容》，作为短文摘要。

**格式**（AI 必须遵守）：

```plaintext
## 你需要查看与特别关注的内容

- [ ] **待确认清单**：第 1 条「…」需你选 A/B/C；第 2 条「…」…
- [ ] **实现约束**：第 3 条「…」可能与既有 XX 冲突，请确认
- [ ] **Config 检查**：interactableLayer 必选 Interactable；animationClip GUID 需与 .meta 一致
- [ ] **验证项**：第 4 条「跳跃时按钮跟随」— 请确认这是你的验收标准
…
```

**要求**：

- 只列**你必须查看或决策**的项，不列可跳过内容

- **本块内须写出用户需决策/确认的完整内容**（待确认项含完整 A/B 方案与推荐；实现约束、Config 检查、验证项为完整条文或自包含摘要），**不得仅写「见 STEP x.x」或条目标题**，以便用户仅阅读本块即可完成确认、无需翻阅后文

- 每项可多行，可附带原文位置；用 `- [ ]` 方便你勾选

- 产出后你只需看这一块，不必在长文档里找

**在以下步骤的 Prompt 中，AI 必须被告知此要求**：STEP 0.6、STEP 1、STEP 8.5（见各步骤补充）

────────────────────────────────────

# 项目既有架构前置约束（可选但强烈推荐）

如果项目已经存在统一分层与文件结构（如 Presentation / Logic / Infrastructure 三层，或固定目录规范），\
建议在每次流程开始前声明一次“架构前置约束”。

用途：

- 避免 AI 生成与当前工程冲突的结构

- 保持长期一致性

- 强化架构稳定性

建议固定声明内容（不超过 8 行）：

【当前分层结构】

- 表现层：……

- 逻辑层：……

- 基础设施层：……

【必须遵守】

- 不新增新的顶层层级

- 不破坏既有依赖方向

- 不改变现有入口结构

【允许扩展】

- 允许新增模块

- 允许新增服务

- 允许新增领域对象

注意：

- 这里只声明“约束”，不描述具体实现

- 这是对 AI 的结构边界说明

- 不属于功能卡片内容

**本项目当前值**（建议在 .cursor/rules 或单独 doc 中维护，每次流程前引用）：

【当前分层结构】

- 表现层：Presentation（Views、GameController、Interaction、Input）
- 逻辑层：Logic（Gameplay、Managers）
- 基础设施层：Infrastructure（Services、DataAccess、Core）

【目录映射】见「项目级架构 × 功能级系统的嵌入规则」第 2 条

【交互与靠近提示 Tag】（与 `InteractionController` / `TriggerDetector` 一致）

- `InteractableText`：进入触发范围后，在**可交互物世界坐标**旁显示靠近文案（`PromptViewController`）；文案来自 `InteractableData` + `InteractionConfig`（与表模式一致）。
- `InteractableButton`：靠近显示按钮并可点击播动画；可与同物体上的 `InteractableData` 叠加上方提示。
- `InteractablePlayerText`：数据与解析规则与 `InteractableText` **相同**，由触发器自动显示/隐藏，**不**需要在业务脚本里调用 `DialogueViewController.Show`；呈现位置为**当前激活玩家躯体头顶**（`DialogueViewController`，`dialogueWorldOffset` / `dialogueTopViewport`）。可与 `InteractableText` **同时**出现（两套 UIDocument）。
- 配置检查：物体在 `InteractionConfig.interactableLayer` 内、Collider `IsTrigger`、挂 `InteractableData`（及表中 key 等），按需求选 Tag。

────────────────────────────────────

# 项目级架构 × 功能级系统的嵌入规则（强烈推荐）

目的：

- 让每次 AI 产出的“功能级架构”稳定落在你现有项目架构内

- 防止为了某个功能引入新的顶层结构或破坏依赖方向

## 1) 依赖方向（硬规则）

- 表现层（Presentation）可以依赖：逻辑层接口、基础设施接口

- 逻辑层（Logic）可以依赖：基础设施接口、纯数据模型

- 基础设施层（Infrastructure）不得依赖：表现层、具体玩法逻辑

禁止：

- Logic 直接引用 UI 组件或 View

- Infrastructure 引用 Gameplay/Manager 的具体实现

- 任何层级出现循环依赖

## 2) 模块落盘位置（按你现有目录映射）

- 表现层模块 → `Assets/_Project/Scripts/Presentation/...`

  - Views / ViewBinder / InputAdapter / Interaction 的表现部分

- 逻辑层模块 → `Assets/_Project/Scripts/Logic/...`

  - Gameplay（规则/系统/状态机）

  - Managers（流程/场景/UI 流转）

- 基础设施模块 → `Assets/_Project/Scripts/Infrastructure/...`

  - Services（EventBus/Asset/Audio/Persistence/Logging/Pooling/Loading）

  - Core（ServiceLocator/Time 等）

  - DataAccess（Config/Localization 访问）

- 公共工具与常量 → `Assets/_Project/Scripts/Common/...`

## 3) 功能系统的“最小占用面”

每新增一个功能系统，默认只允许新增：

- 1 个逻辑编排模块（Orchestrator/Controller/UseCase 之一）

- 0–2 个规则/状态模块（StateMachine/Rules）

- 0–1 个数据配置入口（Config/表；表可指 ScriptableObject、JSON 等）

- 0–1 个表现模块（View + Binder）

超出数量必须说明理由（否则视为过度设计）。

## 4) Service 的扩展原则

- 能复用现有 Service 就不新增

- 新增 Service 必须满足：跨多个功能可复用

- 单功能专用能力优先做成：Logic 模块 + 接口 + 由现有 Service 提供基础能力

## 5) 命名与边界一致性（建议）

- 功能级模块命名要反映“职责”，不要反映 Unity 实现

- 允许在 STEP7（接口合同）之后再落到具体 Unity 组件/实现名

## 6) 在每次流程开始时加一句固定声明（给 AI）

> 本项目采用 `Presentation / Logic / Infrastructure` 三层架构与既有目录规范。\
> 本次功能产出必须落在现有层级内，不新增顶层，不破坏依赖方向。

## 7) Unity 项目通用实践（设计时参考）

- **Tag**：优先使用 Unity 自带（Player、Respawn、MainCamera），减少新建
- **Physics**：OnTriggerEnter 依赖 Layer 碰撞矩阵，需在 Project Settings 中检查相关 Layer 是否可触发
- **2D/3D**：碰撞体、刚体类型需与项目一致（3D 用 BoxCollider，2D 用 BoxCollider2D）
- **Config 与节奏**：涉及多段时序、表现节奏（如死亡/复活延迟、无敌时间、VFX 时长）时，建议在 Config 中集中配置各段时长，避免在预制体或代码中写死，便于策划调参与后续扩展

────────────────────────────────────

# 原始功能卡片模板（你唯一需要手写的部分）

说明：

- 你写原始功能卡片，表达业务意图即可

- 不需要完整，不涉及实现，后续由 AI 补全

- 写完后交给 AI 执行 STEP 0

请使用以下结构：

【功能名称】\
一句话名称（如：死亡与复活系统）

【一句话描述】\
从玩家视角描述该系统做什么

【玩家可见行为】（3–6 条）\
只写玩家看到或能做的事

【触发方式】（3–5 条）\
写什么情况下触发

【核心规则】（2–5 条）\
写关键限制或基本逻辑

【不做的事】（2–5 条）\
写清范围边界（如：不做网络同步）

禁止出现：

- Unity API

- 组件名

- 方法名

- 数据结构名

- 优化策略

- 架构设计

这是整个流程唯一的人类输入。

────────────────────────────────────

────────────────────────────────────

# STEP 0 — 功能卡片缺口扫描（AI 自动找漏）

> **可从这里学到**：需求不是一次写全的，缺口扫描是补全的前提。

Output：Document

Prompt（固定使用）：

你是一名资深 Unity 系统设计审查员。

仅基于“原始功能卡片”，输出《缺口清单》，用于补全需求与边界。

请按以下分类列出缺失项，每类最多 5 条，每条不超过 15 字：

1. 目标与范围缺口

2. 触发与输入缺口

3. 输出与反馈缺口

4. 规则与边界缺口

5. 异常与失败路径缺口

6. 扩展预留缺口

7. 数据与配置缺口（若涉及优先级、顺序、多选一、共享配置等，需明确数据来源与形态）

8. **实现风险缺口**（可能引发实现阶段 bug 的未决项，如：UI 定位更新频率、资产引用失效处理、Config 默认值、与相机/物理的时序；**涉及 OnTriggerEnter/碰撞检测时**：Physics Layer 碰撞矩阵是否允许触发；**涉及 Tag/Layer 时**：是否优先使用 Unity 自带（Respawn、Player）；**涉及「首个」「默认」等排序时**：排序规则是否明确（order、position 等）；**涉及「仅首次触发」或「默认项不触发」的反馈时**：需在缺口中标出是否存在「默认项不触发反馈」（如 order 0 不播）、「仅首次激活时播」等规则；若存在，缺口应写明「默认/首次的判定方式待定（如：按 order、按预激活状态）」，以便 STEP 0.6/STEP 1 补全；**涉及瞬移/传送时**：CharacterController 位置变更与 Physics.SyncTransforms() 的调用时机（同帧 vs 下一帧）可能触发引擎内部警告（如 JobTempAlloc），需在实现约束或交接清单中注明建议做法；**涉及协程多段等待时**：各段等待对应的 Config 字段与执行顺序需明确，避免「恢复控制」与「VFX 时长」等混在一起导致漏配或顺序错）

要求：

- 中文输出

- 只指出“缺什么”

- 不提供实现方案

- 不出现 Unity API、组件名、方法名、数据结构名

- 保持简洁

**Output 补充**：产出时必须在开头附带《你需要查看与特别关注的内容》，列出：第 8 类「实现风险缺口」的条目（若有）；其余类别若有与你预期不符的，可顺带标出。

────────────────────────────────────

# STEP 0.5 — 增强版功能卡片生成（AI 自动补全）

Output：Document

Prompt（固定使用）：

基于“原始功能卡片”与《缺口清单》，生成《增强版功能卡片 v1》，作为后续步骤唯一输入。

必须严格使用以下结构：

【功能一句话】

【目标】

【用户可见行为】（最多 6 条）

【输入/触发】（最多 6 条）

【输出/反馈】（最多 6 条）

【规则与边界】（最多 8 条）

【异常与失败路径】（最多 6 条）

【不做的事】（最多 5 条）

【可扩展预留】（最多 4 条）

规则：

- 信息不足时用“待确认：…”占位（最多 4 处）

- 禁止出现 Unity API、组件名、方法名、数据结构名

- 若缺口清单含数据与配置缺口，需在规则与边界中补全

- 中文输出

- 保持简洁

**Output 补充**：产出时必须在开头附带《你需要查看与特别关注的内容》，列出：所有「待确认」占位及其所在位置；规则与边界中若有需你拍板的（如优先级数据来源、表形态），单独标出。

────────────────────────────────────

# STEP 0.6 — 待确认事项与方案建议（AI 产出，供你决策）

> **你需查看**：本步骤**必须由你确认**后再继续；每条都是决策点。\
> **可从这里学到**：关键决策需要人拍板，AI 提供方案，你选。

Output：Document（供用户快速勾选/回复）

目的：将“待确认”和“可选方案”集中呈现，避免多轮来回，加速决策。

Prompt（固定使用）：

基于《增强版功能卡片 v1》及其中“待确认”占位、缺口清单、STEP 1 将涉及的关键决策，输出《待用户确认清单》。

要求：对于**需要你（用户）补充或确认**的每一条，AI 必须同时提供：

1. **问题简述**（1 句，说清要决定什么）

2. **2–3 个可选方案**（各 1–2 句，含简要优劣）

3. **推荐方案**（1 句理由）

格式示例：

| #   | 问题                        | 方案 A | 方案 B     | 方案 C | 推荐 | 理由           |
| --- | --------------------------- | ------ | ---------- | ------ | ---- | -------------- |
| 1   | Scriptable 表新建还是扩展？ | 新建表 | 扩展现有表 | —      | A    | 解耦、职责清晰 |

规则：

- 仅列出**必须由用户拍板**的项（最多 6 条）

- 方案用业务语言描述，不出现 Unity API、组件名

- 若某项已可从增强卡片推断，不必列入

- 你回复「按建议来」或逐条确认后，AI 将结论写入设计文档，再进入 STEP 1

**确认完成后的同步要求**：AI 需根据确认结果，更新以下内容并保持全文一致：
- STEP 0.5 增强卡片的「规则与边界」中相关条目
- STEP 7 接口合同（如 Config 新增字段、接口参数）
- STEP 8.5 实现交接清单（模块要点、Config 检查、验证项）
- 若确认项影响状态机，则同步 STEP 3 状态模型

**确认状态记录**：确认后应在文档中显式记录，格式示例：
- 待确认清单：`**你的选择**：第 1 条选 **A** / 第 2 条选 **A** / 第 3 条选 **B** ✓ 已确认`
- 其他区块：`**确认状态**：✓ 已确认` 或 `**确认状态**：✓ 已确认（按建议采纳）`

**Output 补充**：产出时必须在开头附带《你需要查看与特别关注的内容》，列出本步骤每一条待确认项。**待确认项必须写出完整 A/B 方案内容**（含优劣），而非仅写「第 n 条需选 A/B」。格式示例：`- [ ] 第 1 条「死亡区域形态」：**A** 触发器+Layer（优：批量管理；劣：需新建 Layer）| **B** 触发器+Tag（优：无需新建 Layer；劣：Tag 有限）— 推荐 A`。供用户快速勾选，无需在长表格中查找。

────────────────────────────────────

# STEP 1 — 设计审查（轻量）

> **你需查看**：**「实现约束」**必看；关键决策若与既有系统冲突，需你指出。

Output：Document

Prompt：

【系统约束声明】\
本项目采用 Presentation / Logic / Infrastructure 三层架构。\
功能必须落在现有层级内。\
不得新增顶层结构。\
不得破坏依赖方向。\
不得产生循环依赖。

输出要求：

- 中文

- 简洁

- 禁止过度分析

- 不生成与本功能无关的结构

你是一名资深 Unity 技术架构师。

仅基于《增强版功能卡片 v1》，进行轻量级设计审查：

1. 列出缺失的关键架构决策（最多 5 项）

2. 列出可能的扩展风险（最多 3 项）

3. 列出可能的性能风险（仅在必要时）

4. 为每个关键决策提供 2–3 个可选方案

5. 若规则涉及优先级、顺序、多选一、表驱动逻辑，需明确数据表/Config 的形态（如 ScriptableObject、扩展现有表等）

6. **列出实现约束**（最多 5 条）：与本功能直接相关的技术前提，用业务语言描述。若涉及 UI 定位、资产引用、ScriptableObject、Config、相机/渲染，必须至少列出 1 条。例：「按钮 UI 需随相机/目标移动持续更新位置」「资产引用失效时视为无动画」「Config 的 interactableLayer 需选 Interactable」「相机跟随时 UI 位置需平滑以避免抖动」。**涉及 OnTriggerEnter 检测时**：需检查 Physics 碰撞矩阵，确保相关 Layer 可触发；**涉及「首个」「默认」时**：需明确排序规则（如 order 字段、position.x）；**涉及「默认项不触发反馈」或「仅首次激活才触发反馈」时**：实现约束中需写明 (1) 默认的判定方式（如 order == 0、某 ID、或 Config 预设条目）；(2) 采用「传参判定」（如传 order，仅当 order != 0 时播）还是「状态判定」（如预激活默认点，重复经过同一处不播）；(3) 若与存档/读档相关，需说明读档后「当前检查点」与默认的关系；**涉及状态切换（如死亡）时**：需列出所有需禁用/恢复的 Controller（如 Movement、Interaction、PlayerToggle）。**涉及多步状态切换时**：除列出需禁用/恢复的组件外，应注明**步骤顺序**（如：先禁用 Controller → 播 VFX → 等待 A → 瞬移 → 播 VFX → 等待 B → 恢复 Controller → 启动无敌计时），以及每步依赖的 Config 字段（如 deathToRespawnDelay、respawnToControlDelay），便于实现交接清单与验证项一致。

要求：

- 中文输出

- 保持结构清晰但简洁

- 不生成完整架构

- 不展开实现细节

你只需要从中选择关键决策，不必全部手动写。

**Output 补充**：产出时必须在开头附带《你需要查看与特别关注的内容》，列出：1）实现约束（**逐条写出完整内容**，不得仅写「见 STEP 1」或条目标题）；2）若有与既有系统/镜头/Config 冲突的决策，单独标出需用户确认。其余可省略。

────────────────────────────────────

# STEP 2 — 系统边界建模

Output：Document（推荐）或 Diagram

Prompt：

【系统约束声明】\
本项目采用 Presentation / Logic / Infrastructure 三层架构。\
功能必须落在现有层级内。\
不得新增顶层结构。\
不得破坏依赖方向。\
不得产生循环依赖。

输出要求：

- 中文

- 简洁

- 禁止过度分析

- 不生成与本功能无关的结构

基于《增强版功能卡片 v1》与已选择的决策，生成系统边界说明。

输出必须包含：

1. 系统内部（三层结构）

   - 表现层：2–3 个职责单元

   - 逻辑层：2–3 个职责单元

   - 数据层：1–2 个职责单元（可含 Config、表/ScriptableObject 等）；若需求涉及表驱动（优先级、顺序等），需明确对应模块\
     总数不超过 8 个

2. 外部依赖（最多 6 项）

3. 系统负责什么（3–5 条）

4. 系统不负责什么（2–4 条）

规则：

- 仅描述职责

- 不写 Unity API、组件名、方法名、数据结构名

- 不描述实现策略

- 中文输出

────────────────────────────────────

# STEP 2.5 — 架构卡片收敛（Sticky）

Output：Sticky Notes

Prompt：

【系统约束声明】\
本项目采用 Presentation / Logic / Infrastructure 三层架构。\
功能必须落在现有层级内。\
不得新增顶层结构。\
不得破坏依赖方向。\
不得产生循环依赖。

输出要求：

- 中文

- 简洁

- 禁止过度分析

- 不生成与本功能无关的结构

基于 STEP2 的系统边界说明，将系统内部职责收敛为 6–8 张架构卡片。

每张卡片固定三行：

卡片名：\
输入：\
输出：

按三组摆放：

- 表现层

- 逻辑层

- 数据层

禁止实现细节。

────────────────────────────────────

# STEP 3 — 状态模型

Output：Diagram

Prompt：

生成该功能的状态机图。

包含：

- 核心状态

- 状态转换

- 触发事件

- 是否需要持续更新

要求：

- 简洁

- 可直接指导实现

- 中文输出

────────────────────────────────────

# STEP 4 — 模块架构图

Output：Diagram

Prompt：

【系统约束声明】\
本项目采用 Presentation / Logic / Infrastructure 三层架构。\
功能必须落在现有层级内。\
不得新增顶层结构。\
不得破坏依赖方向。\
不得产生循环依赖。

输出要求：

- 中文

- 简洁

- 禁止过度分析

- 不生成与本功能无关的结构

生成分层模块架构图。

使用三层结构：

- 表现层

- 逻辑层

- 数据层

标明：

- 模块名称

- 职责

- 依赖方向

避免循环依赖。

────────────────────────────────────

# STEP 5 — 职责矩阵（可选）

Output：Table

Prompt：

生成职责矩阵表。

列：

- 模块名称

- 所属层级

- 职责

- 依赖

- 对外接口

保持简洁。

────────────────────────────────────

# STEP 6 — 数据流图（可选）

Output：Diagram

Prompt：

生成数据流图。

标明：

- 事件来源

- 状态变化

- 数据读取

- UI 更新

- 输出位置

保持简洁。

────────────────────────────────────

# STEP 7 — 接口合同

Output：Document

Prompt：

【系统约束声明】\
本项目采用 Presentation / Logic / Infrastructure 三层架构。\
功能必须落在现有层级内。\
不得新增顶层结构。\
不得破坏依赖方向。\
不得产生循环依赖。

输出要求：

- 中文

- 简洁

- 禁止过度分析

- 不生成与本功能无关的结构

为各模块生成接口合同。

每个接口包含：

- 名称

- 方法

- 参数

- 行为说明

- 不允许依赖的层级

- 数据层若含表/ScriptableObject，需补充：Entry 结构、字段说明、与组件的对应关系；**表模式时组件 Inspector 应隐藏表中已有字段**（避免重复填写），可自定义 Editor 实现

- **接口类型在 Inspector 的常见做法**：Logic 层定义接口（如 IDeathVFX）、Presentation 层实现时，Unity 无法直接序列化接口类型；应在组件上暴露**实现该接口的 MonoBehaviour 引用**，在代码中转为接口使用；实现交接清单中可注明「用实现类引用，运行时转为接口」

- **可选反馈的调用条件**：若接口表示「可选反馈」且存在「仅部分情况触发」（如仅新激活、仅非默认项），接口合同或实现交接清单中应写明**调用条件**（例如「仅当新激活且 order != 0 时由 Controller 调用」），避免实现时只实现「能调」而漏掉「何时不调」。

逻辑层接口不要依赖 Unity API。

────────────────────────────────────

# STEP 8 — 实现计划

Output：Kanban

Prompt：

【系统约束声明】\
本项目采用 Presentation / Logic / Infrastructure 三层架构。\
功能必须落在现有层级内。\
不得新增顶层结构。\
不得破坏依赖方向。\
不得产生循环依赖。

输出要求：

- 中文

- 简洁

- 禁止过度分析

- 不生成与本功能无关的结构

生成实现任务看板。

列：

- 待办

- 进行中

- 测试

- 完成

任务要求：

- 原子化

- 有验收标准

- 符合分层结构

- 若涉及数据表/Config，需包含新建或扩展 ScriptableObject/表 的任务

- 若复用既有预制体、UIDocument、场景对象等，需列出「既有资源约束」及引用方式

────────────────────────────────────

# STEP 8.5 — 实现交接清单（AI 产出，实现阶段必读）

> **你需查看**：**Config/资产检查清单**、**实现后验证项** 必须符合你的验收标准；不符需让 AI 修改后再实现。\
> **可从这里学到**：实现前要有可执行的检查清单，验收才有据可依。

**目的**：将设计转化为 AI 可直接执行的实现指南，减少遗漏与返工。

Output：实现交接清单（单份文档）

Prompt（固定使用）：

基于 STEP 1–8 的全部产出（含实现约束），生成《实现交接清单》，供实现阶段 AI 或人类直接使用。

必须包含三部分：

**1. 模块与关键实现要点**

| 模块 | 文件路径 | 创建/修改 | 关键实现要点 |
| ---- | -------- | --------- | ------------ |
| …    | …        | …         | …            |

关键实现要点：从 STEP 1 实现约束、STEP 7 接口合同提炼，写清易遗漏或易错的技术细节（如更新频率、平滑参数、引用校验）。若功能包含**多段时序**（如：死亡 → 等待 → 瞬移 → 等待 → 恢复控制），应按时间顺序列出每段并标明对应 Config 字段（如 deathToRespawnDelay、respawnToControlDelay），避免实现时漏步骤或用错字段。**依赖引用**：优先 Inspector 配置，避免 Find 兜底；保留同物体 GetComponent、Camera.main，不推荐 FindAnyObjectByType 做依赖注入。**表驱动组件**：当 Source=Table 时，Inspector 应隐藏表中已有字段（如 buttonText、promptText、animationClip、defaultStateName），需自定义 Editor。**若存在「默认项」或「仅首次触发」的反馈**：交接清单中应写明 (1) 默认/首次的判定方式（如 order、ID 或状态）；(2) 表现层调用反馈的准确条件（如 isNewActivation && order != 0）；(3) 若使用 Inspector 拖引用，是否需要在未拖时做同物体/同层级自动查找及查找规则，避免「拖了但没拖对槽位」导致不触发。

**2. Config/资产检查清单**

列出必填项、推荐值、常见错误及后果。例：interactableLayer 必选 Interactable；animationClip 的 GUID 需与 .anim.meta 一致；依赖引用优先 Inspector 配置，避免 Find 兜底。表模式时 Inspector 隐藏表中已有字段（可自定义 Editor）。若存在「表现时长/节奏」类配置（如 VFX 播放时长、恢复控制延迟），建议在清单中注明：**与节奏相关的时长优先放在 Config 中统一配置**，便于与玩法节奏一致、减少预制体与代码写死。**通用检查项**：Tag/Layer 优先使用 Unity 自带（Respawn、Player）；涉及 Trigger 检测时，需检查 Physics 碰撞矩阵；2D/3D 项目需明确碰撞体类型（BoxCollider vs BoxCollider2D）。

**3. 实现后验证项**

3–6 条可执行的验收场景。例：靠近时按钮显示且位置正确；跳跃时按钮位置跟随；按钮无明显抖动；引用错误时按钮不显示。

**与功能文档的对应**：若功能文档采用「需确认内容统一放最前」结构，则文档最前的「六、实现后验证项」应与本步骤的「实现后验证项」**内容一致**，仅位置不同（前者在文档最前，后者在交接清单中）。避免两处验证项不一致导致验收混乱。

规则：

- 业务语言与技术细节混合，允许出现必要的方法名、文件路径

- **实现前确认**：实现阶段 AI 必须先检查待确认清单、实现约束、Config/验证项是否均已标注确认；若有未确认项，列出并询问用户，待补齐后再编码。**开始执行前**必须与用户确认是否开始执行实现，待用户明确同意后再动手编码。

- 实现阶段 AI 必须以本清单 + 接口合同 + 增强卡片 为准

- 中文输出，保持简洁

**Output 补充**：产出时必须在开头附带《你需要查看与特别关注的内容》，列出：1）Config/资产检查清单中的必填项与常见错误（**写出完整内容**，不得仅写「见 STEP 8.5」或条目标题）；2）实现后验证项（**逐条写出完整验收内容**）。用户仅看此块即可确认是否符合预期，无需翻阅完整清单。

────────────────────────────────────

# 使用强度建议

| 场景               | 建议步骤     |
| ------------------ | ------------ |
| 快速实验           | STEP 0 → 4   |
| 标准系统           | STEP 0 → 7   |
| 核心系统（含实现） | STEP 0 → 8.5 |

实现时：将《实现交接清单》+《增强版功能卡片》+《接口合同》一并交给 AI，并说明「按实现交接清单实现，验证项需通过」。

**STEP 5/6 何时用**：多数功能 0→8.5 即可。模块数 ≥ 5 或职责易混时，可加 STEP 5 职责矩阵；事件流、多模块协作复杂时，可加 STEP 6 数据流图。

**功能文档结构建议**：当产出单功能设计文档（如《死亡复活系统-开发文档》）时，建议将以下内容**统一放在文档最前面**，便于用户一次完成确认。**放在最前的必须是完整内容（或自包含摘要），而非仅条目标题 +「见 STEP x.x」指向**；用户仅读最前块即可完成全部确认。

1. 待确认清单（含完整 A/B 方案与「你的选择」填写处）
2. 实现约束（完整条文）
3. Config/资产检查清单（完整必填项与常见错误）
4. 实现后验证项（完整验收条）
5. 后续扩展预留（若有，如存档/检查点、音效接口等）
6. 各区块末尾增加「**确认状态**：✓ 已确认」或「**你的选择**：… ✓ 已确认」

7. **STEP 0 缺口清单**：若缺口已在 STEP 0.5、0.6、STEP 1 中补全，正文中的缺口清单需标注「**缺口清单（已补全）**」，并在表格前注明：`> 以下缺口已在 STEP 0.5 增强卡片、STEP 0.6 待确认清单、STEP 1 实现约束中补全。`

8. **实现完成后可选产出**：参考《光圈系统》《相机系统》等模块文档格式，为该功能单独写一份「XXX 系统模块（设计文档 + 批判性分析）」复盘文档，包含：① 设计概述与变更说明；② 遇到的问题与解决（现象/原因/解决）；③ 反思与经验。复盘时可记录**边界行为与默认值**的决策与实现方式（例如：默认检查点是否播反馈、首次激活的定义、未配置引用时的降级策略），便于后续类似功能或新人接手时一致实现。

9. **使用说明文档**：实现完成后，由 AI 产出该功能的《XXX 使用说明》（可与设计文档同目录或放在 `Assets/_Project/Docs/`），便于日后单独查阅、不依赖对话记录。建议包含：① 功能一句话与入口（在场景/预制体中的挂载与触发方式）；② Inspector 配置项说明（必填项、可选项、常见填错后果）；③ 与相关模块的衔接（如依赖的 Config、Tag、Layer）；④ 常见问题与排查（如「不触发」「表现异常」时的检查顺序）。实现阶段结束时可将「产出使用说明文档」列入待办，或单独让 AI 根据实现交接清单与最终代码补写。

确认完成后，再进入「以下为完整设计文档」的 STEP 0–8.5 正文。

────────────────────────────────────

# 协作节奏

- **一次对话完成**：STEP 0 → 0.5 → 0.6，你确认后再继续

- **设计阶段**：STEP 1 → 8 可连续产出，你按需抽查

- **设计完成**：当 0.6 已确认、实现约束已看过、交接清单的 Config/验证项没问题时，回复「可进入实现」即可

- **实现阶段**：单独对话，把交接清单 + 设计文档作为上下文，交给 AI 实现；**长文档时**：优先提供《实现交接清单》全文 + 《接口合同》要点 + 《增强卡片》规则与边界，完整设计可当附件。**AI 收到后**：先检查待确认清单、实现约束、Config/验证项是否均已确认；若有遗漏，先询问你补齐；**然后必须先与你确认是否开始执行**（例如简要列出即将执行的步骤），待你明确同意后再开始编码，不得未经你确认直接动手改代码。

**验证失败时**：

- 清单有、代码没做 → 让 AI 按清单补全

- 清单不对、和预期不符 → 回到 STEP 1 或 8.5 改约束/交接清单，再实现

- **表现与预期不符**（如某反馈不触发、重复触发、默认项也触发）：先对交接清单与实现约束，检查是否写清「默认/首次」的判定与调用条件；若未写清，先在设计文档中补充约束或交接要点，再让 AI 按更新后的清单修正实现，而非只改代码不更新文档

- 需求理解错了 → 回到 STEP 0.5/0.6 改增强卡片，再重新设计

- 看不懂某一步时，直接问 AI：「这一步在做什么？我该如何判断？」

**需求变更后的最小回滚**：

- 若仅 STEP 0.6 选择有误：从 STEP 0.6 修正选择 → 同步 STEP 0.5/7/8.5
- 若 STEP 0.5 规则有误：从 STEP 0.5 重跑 → 0.6 → 1 → 8.5（2–4、7 可视情况跳过或局部更新）
- 若 STEP 1 实现约束有误：从 STEP 1 重跑 → 8.5

────────────────────────────────────

# 附录：与业内流程的对应

学习时建立映射，未来进团队时不脱节：

| doc2 概念      | 业内常见叫法                    | 团队里通常谁负责 |
| -------------- | ------------------------------- | ---------------- |
| 原始功能卡片   | PRD 初稿 / User Story           | 产品 / 策划      |
| 缺口清单       | 需求评审、Gap Analysis          | 产品 + 技术      |
| 增强版功能卡片 | PRD 定稿 / 功能规格             | 产品             |
| 待确认清单     | 决策点、Stakeholder 评审        | 产品 + 技术 Lead |
| 实现约束       | 非功能需求、技术前提            | 架构 / 技术      |
| 实现交接清单   | Tech Spec、Implementation Brief | 技术             |
| 验证项         | 验收标准、Acceptance Criteria   | 产品 + QA        |
